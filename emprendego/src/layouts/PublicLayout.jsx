// Layout para páginas públicas (tienda del cliente)
// Se implementará en una fase posterior

const PublicLayout = ({ children }) => {
  return (
    <div className="min-h-screen bg-white">
      {children}
    </div>
  )
}

export default PublicLayout
